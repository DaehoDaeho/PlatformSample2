using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class aaaa : MonoBehaviour
{
    int a;
    float b;
    string c;
    // Start is called before the first frame update
    void Start()
    {
        // 게임이 실행되면 처음에 한 번 호출되는 함수.
        // 데이터 초기화.
    }

    // Update is called once per frame
    void Update()
    {
        // 매 프레임마다 수행
        // 입력 처리
        // 게임 상태 업데이트        
    }
}
